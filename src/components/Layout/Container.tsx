import { styled } from '../../style';

export const Container = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  lineHeight: '$default',
  fontSize: '$default',
  fontWeight: '$default',
  color: '$default',
});
